expname: object

model:
  stage: object
  use_gan: false
  color: false

  lambda_gan: 1
  lambda_grad: 10
  lambda_recon: 10
  d_reg_every: 16

  optim:
    lr: 1e-3
    lr_dis: 1e-3

  deformer:
    max_steps: 30
    nonlinear_offset: false
    delta_w: false
  
# datamodule:
#   batch_size: 8